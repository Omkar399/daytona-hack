# GitHub Webhook Secret (set this in GitHub webhook settings)
GH_WEBHOOK_SECRET=your_webhook_secret_here

# Optional: GitHub Token for fetching PR comments/reviews
# (needed only if you want to fetch CodeRabbit summaries)
GH_TOKEN=ghp_your_github_token_here

# DevRel API endpoint - triggered when PR is merged
# Local: http://localhost:8000/experiment/from-webhook
# Production: https://your-api.example.com/experiment/from-webhook
SANDBOX_URL=http://localhost:8000/experiment/from-webhook

# Server port
PORT=8080
